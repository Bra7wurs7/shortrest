[package]
name = "shortrest"
version = "0.3.1"
edition = "2024"

[dependencies]
trayserve = { path = "../trayserve" }
rust-embed = "8.7.2"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
