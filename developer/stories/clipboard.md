I want unsaved files to appear in a clipboard.
- When the currently open file does not exist, but a change event happens, an unnamed file is appended to the clipboard and set as the active file
- When I make a change to the open saved file, a changed copy of that file is added to the clipboard
